import org.dao.CatDao;
import org.dao.OwnerDao;
import org.model.Cat;
import org.model.Owner;

import java.time.LocalDate;

public class Main {

    public static void main(String[] args) {
        /*
        Owner owner = new Owner("vasyan", LocalDate.of(2019, 01, 04));
        OwnerDao ownerDao = new OwnerDao();
        ownerDao.saveOwner(owner);
        Owner ow = ownerDao.getOwnerById(5);
        ow.setBirthDate(LocalDate.of(2001, 01, 01));
        Cat cat = new Cat(
                "Murzik",
                LocalDate.of(2023, 12, 02),
                "Kot",
                "White",
                3);
        CatDao catDao = new CatDao();
        catDao.saveCat(cat);
        catDao.saveCat(new Cat("Test", LocalDate.now(), "Test", "Test", 3));
        Cat cat1 = catDao.getCatById(7);
        Cat cat2 = catDao.getCatById(11);
        catDao.addFriendToCat(cat1, cat2);
        ownerDao.saveOwner(new Owner("Petya", LocalDate.of(2003, 11, 15)));
        ownerDao.deleteOwner(ow);
         */
    }
}
